import styled from "styled-components";
import SectionContainer from "./SectionContainer";

const Container = styled.div`
  width: 300px;
  height: 500px;
  border: 1px solid;
`;

function EvolutionChart({ data, lang }) {
  return <SectionContainer id="evolv_chart">evolv_chart</SectionContainer>;
}

export default EvolutionChart;
